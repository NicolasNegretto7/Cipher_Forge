# 8. Control de Cambios

## ¿Para qué sirve este documento?

En Scrum es normal y esperable que el alcance se ajuste durante el proyecto: el
Product Backlog es flexible por diseño. Pero que sea flexible no significa que los
cambios queden sin registro. El control de cambios es una bitácora que deja constancia
de **qué cambió, por qué, quién lo pidió o lo detectó, y qué impacto tuvo** en alcance,
tiempo, costo o backlog.

Este documento es distinto del [Sprint Review](04_sprint_review.md): el Sprint Review
es el acta de la reunión donde surge el cambio; el control de cambios es el registro
consolidado y trazable de todas las decisiones de cambio del proyecto, sin importar su
origen (una reunión, un hallazgo técnico, un pedido fuera de sprint).

---

## Plantilla

| ID | Fecha | Origen | Descripción del cambio | Impacto | Decisión | Estado |
| --- | --- | --- | --- | --- | --- | --- |
| CC-01 | | | | | | |

Donde:
- **Origen:** de dónde surge (Sprint Review N, pedido directo del cliente, hallazgo
  técnico del equipo, restricción externa, etc.)
- **Impacto:** en alcance (historias agregadas/quitadas), tiempo, costo o prioridades.
- **Decisión:** qué resolvió el equipo (aceptar, rechazar, postergar, dividir en fases).
- **Estado:** Propuesto / Aceptado / Rechazado / Aplicado / Postergado.

---

## Registro de Cambios del Proyecto

| ID | Fecha | Origen | Descripción del cambio | Impacto | Decisión | Estado |
| --- | --- | --- | --- | --- | --- | --- |
| CC-01 | Refinamiento inicial | Decisión del equipo / Restricción UTU | Se reemplaza el flujo de autorización de descarga con notificación al fotógrafo por descarga directa en dos niveles de calidad ("Buena Calidad" y "Alta Calidad"). El cobro real queda para una fase posterior por la restricción de minoría de edad y métodos de pago de UTU. | Alcance: Se eliminan del alcance las notificaciones de solicitud y aprobación de descarga. Backlog: Se retiran HU9 (3 pts), HU29 (3 pts) y HU30 (3 pts). El esfuerzo total del backlog se reduce en 9 puntos (de 100 a 91 pts). | Aceptado: Se ajusta a descarga directa e inmediata para la primera versión. | Aplicado |
| CC-02 | Refinamiento inicial | Decisión del equipo | Se descarta la selección múltiple comprimida en archivo .zip en la funcionalidad de descarga (RF10 y HU10). | Alcance: RF10 y HU10 se acotan exclusivamente a la descarga directa e individual por archivo. Se simplifica el backend eliminando el empaquetado dinámico en .zip. | Aceptado: La descarga se gestionará individualmente archivo por archivo. | Aplicado |
| CC-03 | Refinamiento inicial | Decisión del equipo | Se eliminan los requerimientos concatenados a notificaciones y permisos de descarga: RF12 (notificación al fotógrafo), RF22 (modo de selección visual para solicitud) y RF23 (notificación de resolución al usuario). Se desvincula la referencia a RF22 en RF15 (moderación colaborativa). | Alcance: Se depura la épica EP5 (Visualización y descargas), pasando de 18 a 9 puntos asociados. El Product Backlog priorizado se reorganiza de forma correlativa (orden 1 a 29) sin saltos. | Aceptado: Eliminar registros de notificaciones de descarga en requerimientos, épicas y backlog. | Aplicado |
| CC-04 | Refinamiento inicial | Revisión interna del equipo | Corrección de ambigüedad crítica en la cita de la Ley 18.331 (se redactaba erróneamente "contra la protección de datos"), discordancias gramaticales, términos informales ("sub-líder", "métodos de pagos", "Fotografo") y referencias residuales a descargas "autorizadas". | Calidad documental: Mayor rigurosidad conceptual, legal y técnica para la defensa ante el tribunal de UTU. Sin impacto en puntos de historia. | Aceptado: Se aplican correcciones de estilo, coherencia y ortografía en `01-requerimientos.md`. | Aplicado |
| CC-05 | 2026-09-08 | Decisión técnica del equipo | Se reformula la historia de usuario HU18 en `01-requerimientos.md` (sección 19). Se acota el enunciado de "Como fotógrafo, quiero editar mi información de perfil profesional y figurar en el directorio general de descubrimiento y eventos públicos, para que nuevos clientes puedan encontrarme y contactarme." a "Como fotógrafo, quiero editar mi información de perfil, para tener siempre mis datos actualizados.", en línea con la remoción técnica del campo `especialidad` de la tabla `fotografos`. | Alcance: HU18 conserva sus 3 puntos y su vinculación a RF19 y a la épica EP2 (Perfiles de fotógrafos y directorio). Sin impacto en tiempo ni costo. | Aceptado: La edición de perfil se mantiene operativa (nombre usuario y teléfono). | Aplicado |
| CC-06 | 2026-09-09 | Revisión Technical Writer / Project Manager (solo cambios pequeños) | Correcciones ortográficas y de redacción en `01-requerimientos.md` sin cambio de significado: RF16 `rederigira` → `redirigirá`; RF24 doble espacio `los  Términos` → `los Términos`; RF25 `restriccion/maxima` → `restricción/máxima`; criterios RF26 `-La cantidad maxima...sera` → `- La cantidad máxima...será`; EP2 `Modificacion` → `Modificación`; §17 `Se continua` → `Se continúa`; HU14 `(en la coleccion) o otros` → `(en la colección) u otros`; backlog HU22 `coleccion` → `colección`; backlog HU23 `pública` → `públicos` (concordancia de género). | Calidad documental: sin impacto en alcance, tiempo, costo ni puntos de historia. | Aceptado: cambios pequeños permitidos sin aprobación previa. | Aplicado |
| CC-07 | 2026-09-09 | Revisión Technical Writer / Project Manager (auditoría Scrum y trazabilidad) | PROPUESTO - pendiente de aprobación: (A) Ambigüedades RNF2/RNF3/RNF4/RF9/RF7/RF25-RF26; (B) Exceso de especificidad RF6/RF9/RF15/EP4/HU `Como sistema`; (C-D) Contradicciones/trazabilidad (visión cobro ágil, charter, RF12, SR-04/SR-05, backlog HU22, RF11 duplicado, velocidad, 05/06 vacíos, enlaces README/ética, Reviews sin cliente). | Si se acepta: reformulación RF/RNF/HU/épicas y actualización de secundarios. Sin cambio de código. | Decisión 2026-09-09 del Product Owner: se aprueba solo B completo + C-visión (ver CC-08). El resto (A y resto de D) queda validado y aceptado como está, sin modificación. | Aceptado parcial |
| CC-08 | 2026-09-09 | Aprobación Product Owner (B + C-visión) | Cambios GRANDES autorizados y aplicados en `01-requerimientos.md`: (B1) RF6: se elimina `mediante lógica de backend`; (B2) RF9: se elimina `mediante una librería especializada`; (B3) RF15: se generaliza (se eliminan `modo de selección visual`, detalle en base de datos, texto literal del aviso y `tarea automática programada`, se conserva regla aprobar + eliminar tras 24h); (B4) EP4: se eliminan `FFmpeg (Docker)`, `Filesystem` y `15 segundos` (detalle en `02-modelado.md`/`infraestructura.md`); (B5) HU reformuladas a valor de usuario Scrum/INVEST: HU13/HU16/HU20/HU28 a `Como fotógrafo`, HU19/HU21/HU25 a `Como usuario nuevo`, HU32 a `Como usuario`, HU15 a `Como cliente` (sin `Lemuel Swec`); (C1) Visión §8: `cobro ágil... (Esto último...)` → `(El cobro ágil queda pospuesto a fase posterior por restricción UTU —menores sin pagos reales—, ver Decisiones técnicas)`. | Calidad/metodología Scrum: requerimientos en nivel general (QUÉ, no CÓMO), HU con actor de valor. Sin impacto en puntos (91 pts), tiempo ni código. | Aceptado y aplicado según autorización. Resto de CC-07 validado sin cambios. | Aplicado |
| CC-09 | 2026-09-09 | Pedido Product Owner (cambio pequeño) | Ajuste de redacción en `01-requerimientos.md` §8 Visión: `(El cobro ágil queda pospuesto a una fase posterior...)` → `(El cobro ágil queda pospuesto a una versión posterior...)`. | Calidad documental: sin impacto en alcance, tiempo, costo ni puntos. | Aceptado: cambio pequeño autorizado. | Aplicado |
| CC-10 | 2026-09-09 | Aprobación Product Owner Pasada 1 (3 iteraciones, sectores nuevos) | Cambios pequeños/consistencia en `01-requerimientos.md` (sin cambio de alcance): Alcance §9-5 `MP4 con recorte de 15 segundos` → `MP4`; Alcance §9-6 se elimina `mediante librería especializada` (consistencia con RF9 CC-08); RF21/HU23/HU22/HU5 concordancia género mixto `imagen o video`: RF21 `como favorita` → `como favorita/o`, HU23 igual, HU22 `ya subido` → `ya subido/a`, HU5 `ponerlas` → `ponerlos`; HU4 `archivos multimedias` → `archivos multimedia`; HU11 coma `colección pudiendo` → `colección, pudiendo`; HU12 se elimina `mediante un modo de selección visual` (consistencia con RF15 CC-08). | Calidad/consistencia Scrum: sin impacto en puntos (91 pts), tiempo ni código. | Aceptado: Pasada 1 aprobada. | Aplicado |
| CC-11 | 2026-09-10 | Decisión Product Owner (CF-12, opción A) | Se automatizan respaldo diario y purga colaborativa (RF15/HU12): se agrega el servicio `worker` a `backend/docker-compose.yml` (misma imagen `cipher-forge:dev`, comando `php cron-backup.php --loop`, reinicio `unless-stopped`, volúmenes de código y uploads compartidos), `cron-backup.php` gana intervalos configurables por entorno (`BACKUP_INTERVAL_SEG`/`PURGE_INTERVAL_SEG`, por defecto 86400 s y 3600 s) manteniendo el modo one-shot para cron del host, y se ajustan los archivos Docker: `docker-entrypoint.sh` pasa de `exec apache2-foreground` a `exec "$@"` y el `Dockerfile` declara `CMD ["apache2-foreground"]` (base para reemplazarlo en `worker`). Se documenta en `docs/infraestructura.md` (secciones 1, 2, 3 y 4.2: diagrama, Dockerfile/entrypoint, servicios `mailpit`/`worker` y snippet actualizado) y `docs/02-modelado.md` (nodo BACKUP y capa de almacenamiento/mantenimiento). | Infraestructura: nuevo contenedor en el stack y ajustes de Dockerfile/entrypoint; sin cambios de API ni alcance funcional. | Aceptado: opción A (servicio en compose). | Aplicado |
| CC-12 | 2026-09-10 | Decisión Product Owner (CF-10) | Se aprueba la semántica de descarga en dos calidades ya implementada en el backend: "buena calidad" = copia optimizada Full HD sin marca de agua (`standard/`, con fallback al original si la copia no existe) y "alta calidad" = archivo original (`originals/`), ambas servidas por endpoints que verifican visibilidad y autorización (`MultimediaService::descargar`). **SUPERSEDIDA por CC-24 (2026-09-11):** "buena calidad" pasa a ser el original con calidad baja (JPEG 30) y resolución máxima Full HD (1920 px). | Sin cambios: formaliza un comportamiento ya operativo y verificado por la suite; sin impacto en API ni esquema. | Aceptado: calidades "buena" y "alta" como están definidas. | Aplicado, luego redefinida por CC-24 |
| CC-13 | 2026-09-10 | Decisión Product Owner (CF-15) | Cumplimiento Ley 18.331 (RNF8): (1) consentimiento de políticas en el registro — `clientes.politicas_aceptadas BOOLEAN` + `RegisterDto`/`AuthValidator`/`UserRepository::create` y login con `politicas_aceptadas` para clientes; (2) consentimiento del invitado colaborativo — `multimedia.consentimiento_ts`, `acepto_datos` y aviso legal. Frontend: checkbox de Términos en registro y checkbox legal en colaborativo. **REVERTIDO 2026-09-10 por el Product Owner (todo CF-15):** se revirtió la implementación completa (backend, columnas de BD `clientes.politicas_aceptadas`/`multimedia.consentimiento_ts` eliminadas del esquema y de la BD en ejecución, y UI), quedando el consentimiento del fotógrafo únicamente en el modal HU31 (`fotografos.politicas_aceptadas`, que sigue vigente). La suite de API volvió a su estado previo. | Datos/schema: columna de clientes y columna de multimedia eliminadas con `ALTER TABLE DROP COLUMN`; API vuelve a ignorar `acepta_politicas`/`acepto_datos`. | Aceptado, luego revertido íntegramente el 2026-09-10. | Revertido |
| CC-14 | 2026-09-10 | Decisión Product Owner (CF-08) | Se detectó que `/sistema/backup`, `/sistema/backups` y `/sistema/limpiar-colaborativos` (`routes.php`) son públicos (sin autenticación ni control de rol; el esquema solo admite `fotografo`/`cliente`). Se documenta en `docs/03-seguridad.md` como **cambio a futuro**: exigir clave maestra `SISTEMA_API_KEY` (o alternativa con rol `admin`) antes de exponer el servicio. El `worker` y `cron-backup.php` no se ven afectados porque llaman a los servicios directamente. | Seguridad: riesgo documentado sin código cambiado; sin impacto en API ni esquema. | Aceptado: postergado, documentado como cambio a futuro. | Postergado |

| CC-15 | 2026-09-10 | Decisión del equipo (favoritos solo a colecciones) | En 01-requerimientos.md se cambia favoritos de archivos individuales a solo colecciones públicas completas: Alcance 14 se aclara (no archivos individuales); RF21 pasa de marcar imagen o video a marcar colección pública completa; HU23 igual; Backlog 27 igual; EP5 suma marcado de colecciones como favoritas. En 02-modelado.md se alinea FAVORITOS: pasa de apuntar a multimedia.id_multimedia a colecciones.id, y la relación pasa de MULTIMEDIA a COLECCIONES. | Alcance: se quita favoritos por archivo. Backlog: HU23 conserva 3 pts y Sprint 5. Modelo: FAVORITOS ahora cuelga de COLECCIONES. Requiere alinear código y esquema (favorito_id a colecciones). | Aceptado: decisión del equipo aplicada. | Aplicado |

| CC-16 | 2026-09-10 | Decisión del equipo / Product Owner (CF-NUEVO, límites de subida) | Se detectó que el runtime PHP del contenedor rechazaba cualquier archivo >2 MB (`upload_max_filesize=2M`, `post_max_size=8M` por defecto de `php:8.2-apache`) antes de llegar al código, rompiendo fotos legítimas que la app sí admite (imagen 20 MB / video 800 MB según `MultimediaValidator`). Se agrega `backend/php.ini` inyectado como `99-cipher-forge.ini` en el `Dockerfile`: `upload_max_filesize=900M`, `post_max_size=1G`, `memory_limit=512M`, `max_execution_time=600`, `max_input_time=600`. Verificación: imagen reconstruida, `php -i` confirma 900M/1G/512M, y nuevo check en la suite API (JPG de 3.5 MB → 201, antes 400). | Infraestructura: nuevo archivo de configuración PHP + una línea de `COPY` en el Dockerfile; sin cambios de API ni alcance funcional. | Aceptado por pedido del Product Owner ("extiendelo para que soporten mas"). | Aplicado |
| CC-17 | 2026-09-10 | Hallazgo técnico del equipo (validación F1 QR) | Corrección del motor QR propio en `backend/src/helpers/QrGenerator.php`: la división sintética de `codigoReedSolomon()` omitía el término principal del polinomio generador (bucle `j < grado` en vez de `j < count($gen)`), por lo que los codewords Reed-Solomon no coincidían con el estándar y los lectores independientes (jsQR) no podían decodificar los símbolos. Tras la corrección, los codewords ECC son byte a byte iguales a la referencia (`qrcode`/soldair) y todos los símbolos de prueba (v1, v2, v3 y v7) decodifican la URL correcta; la matriz final coincide celda a celda con la referencia para payloads de modo byte. Se documenta la prueba de escaneo en `docs/06-testing.md`. | Alcance: corrección interna del generador QR; sin cambios de API, esquema ni interfaces públicas de `QrGenerator`. | Aceptado: corrección aplicada y verificada. | Aplicado |
| CC-18 | 2026-09-10 | Implementación técnica de CC-15 (favoritos por colección) | Alineación de código y esquema a la decisión CC-15: `backend/database/schema.sql` cambia la FK de `favoritos.favorito_id` de `multimedia(id_multimedia)` a `colecciones(id)`; `backend/database/migration.sql` agrega bloque idempotente (descarta registros existentes de favoritos por archivo y redefine la clave foránea; igual con cascade desde colecciones). `FavoritoController` opera sobre colecciones públicas (agregar/quitar/listar devuelven una fila por colección con `id_coleccion`, `titulo`, `fotografo_nombre`, `portada_id_multimedia` y `total_archivos`). `ColeccionRepository`: `eliminarConDependencias` purga `favoritos` por `favorito_id = coleccion_id` y `listarPublicas` expone `portada_id_multimedia` (portada por defecto: primera imagen aprobada). Frontend cliente: `panelcliente.js` y `gallery.js` marcan/desmarcan la colección completa (sin proxy de primera imagen), se retira el favorito por archivo de la galería, y `favoritoscliente.js` muestra una tarjeta por colección favorita con un solo `DELETE /favoritos/{id}`. `AUDITORIA_ENDPOINTS.md` refleja la nueva semántica. | Alcance: implementación del comportamiento ya aprobado en CC-15; `migration.sql` descarta los favoritos previos por archivo (semántica obsoleta). Sin cambios de API (mismas rutas `/favoritos`). | Aceptado: CC-15 aplicado en código. | Aplicado |

| CC-19 | 2026-09-10 | Hallazgo técnico del equipo (subida de archivos >2 MB en el panel del fotógrafo) | Corrección del frontend del fotógrafo en `frontend-fotografo/`: al añadir archivos a una colección (`subirimagenes.js`) el flujo guardaba el archivo completo como dataURL en `localStorage`, cuyo límite real del navegador (~5 MB) se alcanzaba con fotos de cámara (>2 MB), lanzando `QuotaExceededError` no controlado y abortando silenciosamente el alta (el archivo "no aparecía" y no se podía seguir subiendo). Solución: los bytes originales se persisten ahora en IndexedDB (nuevo `frontend-fotografo/js/binarios.js` incluido en `SubirImagenes.html`) y en `localStorage` solo queda una miniatura reencodificada (imágenes >2 MB reescaladas a 1600 px JPEG 0.72; videos >2 MB capturan un fotograma como póster). `guardarColeccion()` queda protegido: si la cuota local se agota, sustituye previsualizaciones grandes por un marcador y vuelve a guardar, y `restaurarVistasDesdeBinarios()` devuelve las vistas al recargar. La subida (`subirImagenesPendientes`) usa el File original recuperado en sesión o de IndexedDB (con aviso claro si un original no está disponible). Además se cablea el visor de previsualización (`#visor`) que ya existía en `SubirImagenes.html` pero no tenía manejador de clic: ahora al hacer clic en una miniatura del borrador se abre el lightbox con la vista previa (original si está disponible, con fallback a la miniatura/póster persistida). El backend no cambió (ver CC-16: el transporte ya acepta hasta 900M/1G); verificación en `docs/06-testing.md`. | Alcance: corrección del frontend del fotógrafo (persistencia de borradores y subida); sin cambios de API, esquema ni backend. | Aceptado: corrección aplicada y verificada. | Aplicado |
| CC-20 | 2026-09-11 | Hallazgo técnico del equipo (publicar un borrador da alert genérico "Error de validación.") | Al publicar una colección (`publicarColeccion`), el backend responde `400 {mensaje: "Error de validación.", errores: [...]}` cuando el nombre supera 60 caracteres, la descripción supera 90, o algún archivo no es JPG/MP4 (`ColeccionValidator`/`MultimediaValidator`). El frontend descartaba el array `errores` (`api.js::parsear` solo usaba `mensaje`), dejando sin causa posible al usuario. Corrección en `frontend-fotografo/`: `api.js` propaga `error.errores` y los alerts los muestran con viñetas; además `publicarColeccion` valida ANTES de llamar al backend (nombre obligatorio y ≤60, descripción ≤90, y solo `image/jpeg`/`video/mp4` reales entre los pendientes de subir) con mensajes claros por archivo/campo. La validación de formatos usa detección por cabecera de bytes (magic bytes) del archivo original (`comprobarFormatoReal`): un `premium_photo-*.jpg` de banco de imágenes que en realidad es WebP/PNG, o un `.mov` camuflado, se rechaza en el cliente avisando el formato real antes de llamar a la API. Sin cambios de API ni backend; verificación en `docs/06-testing.md`. | Alcance: frontend del fotógrafo (UX y propagación de errores); sin cambios de esquema ni backend. | Aceptada y verificada. | Aplicado |
| CC-21 | 2026-09-11 | Hallazgo técnico del equipo (colección privada por QR/invitación: imágenes rotas en la galería del cliente) | La galería del cliente (`frontend-cliente/js/gallery.js:crearVista`) usaba `<img/video src="API/multimedia/{id}/vista-previa">`: el navegador no puede adjuntar `Authorization: Bearer` a un recurso `<img>`, y para colecciones PRIVADAS el backend exige sesión → `401` → icono de imagen rota (las públicas funcionaban). Corrección: nueva `window.api.obtenerVistaPrevia(id)` en `frontend-cliente/js/api.js` que hace `fetch` con header Bearer y devuelve un objectURL (mismo patrón que `descargarMultimedia`); la galería y el visor la usan (con liberación de objectURLs al re-renderizar). Verificado de punta a punta: invitado canjea QR de acceso de colección privada, `GET /multimedia/{id}/vista-previa` con Bearer → 200 JPEG (`FF D8 FF E0`, 208977 B); sin token → 401. Sin cambios de backend ni esquema. | Alcance: frontend del cliente (galería/miniaturas/visor con sesión); sin cambios de API ni backend. | Aceptada y verificada. | Aplicado |
| CC-22 | 2026-09-11 | Hallazgo técnico del equipo (los archivos que el fotógrafo agrega a una colección YA publicada no aparecen en el enlace permanente hasta volver a publicar) | En el panel del fotógrafo todo se trabaja sobre un borrador en `localStorage`: los archivos añadidos desde `selectorArchivos` quedaban en local y solo llegaban al backend cuando se pulsaba «Publicar colección» (`subirimagenes.js`). Para una colección ya pública o privada-publicada, eso obligaba al fotógrafo a re-publicar para que los nuevos archivos se vieran en el QR/enlace permanente, y el backend NO tiene caché: `GET /colecciones/{id}/multimedia` lista la BD en vivo (verificado). Corrección (solo `frontend-fotografo/js/subirimagenes.js`): se extrae la validación de formatos reales a `archivosConFormatoInvalido()` y se añade `subirNuevosSiColeccionPublicada()`, que para colecciones con `publicada === true` e id numérico sube de inmediato los archivos **pendientes** (no remotos, aún no subidos) en cuanto se seleccionan y también al cargar la página (recupera pendientes heredados de antes del fix). Si algún archivo no es JPG/MP4 real se avisa y se aborta esa tanda (los válidos quedan listos para «Publicar colección»). La validación de nombre/descripción/visibilidad del botón «Publicar» no cambia; el borrador local se conserva como respaldo. Verificación de punta a punta en `docs/06-testing.md` (colección privada con QR canjeado por invitado: 0 archivos → el fotógrafo sube 2 → el invitado ve 2 al refrescar, sin re-publicar; vista previa del archivo nuevo responde 200). | Alcance: frontend del fotógrafo (subida automática de pendientes en colecciones ya publicadas); sin cambios de API, esquema ni backend. | Aceptada y verificada. | Aplicado |
| CC-23 | 2026-09-11 | Hallazgo técnico del equipo (los JPG descargados para probar descargas se muestran cubiertos de verde en la miniatura del borrador, aunque se ven bien en el visor) | Fallo conocido de Chrome/Edge: al dibujar en un `<canvas>` un JPEG con metadatos de perfil de color (ICC) inconsistentes y luego reencodearlo con `toDataURL`, la imagen resultante aparece toda verde; el `<img>` directo y el visor (que usan los bytes originales) se ven correctos. La miniatura del borrador usa `generarMiniaturaImagen`/`generarPosterVideo` que dibujan en canvas (`drawImage`), por eso solo ella salía verde. Corrección en `frontend-fotografo/js/subirimagenes.js`: nuevo helper `dibujarEnLienzo` que decodifica la fuente con `createImageBitmap({ colorSpaceConversion: "none", imageOrientation: "from-image" })` cuando el navegador lo soporta (ignora el perfil ICC corrupto, trata el RGB como sRGB y respeta la orientación EXIF) y dibuja ese `ImageBitmap`; con fallback automático al `drawImage` directo si la API no está disponible. Se aplica tanto a la miniatura de imágenes como al póster de video. No cambia el backend ni los bytes originales. | Alcance: frontend del fotógrafo (generación de miniaturas/póster, solo visual); sin cambios de esquema ni backend. | Aceptada y verificada. | Aplicado |
| CC-24 | 2026-09-11 | Decisión Product Owner (precisión de la semántica CF-10/HU10) | Se redefine "Buena Calidad" y se **supera la semántica de CC-12**: "buena calidad" = copia limpia sin marca de agua con calidad baja (`MediaProcessor::CALIDAD_BUENA_JPEG = 30`) y resolución máxima Full HD (`MediaProcessor::ANCHO_MAX_BUENA_CALIDAD = 1920`, se reescala solo si el original la supera), servida desde `uploads/standard/`; "alta calidad" = el archivo original íntegro desde `uploads/originals/`. Aplica solo a imágenes; los videos siguen entregándose como original en ambas calidades (no se re-comprimen). Sin cambios de API (`GET /multimedia/{id}/descargar?calidad=buena|alta`) ni de esquema. Verificación E2E en `docs/06-testing.md`: imagen 6000×4000 (2 283 322 B) → buena 1920×1280 a 133 003 B (-94%); imagen 1920×1280 (325 679 B) → buena 1920×1280 a 134 137 B (-59%), SHA-256 distinto. Se actualizan `docs/02-modelado.md` y `AUDITORIA_ENDPOINTS.md`. | Alcance: backend (re-generación de la copia de buena calidad); cambia el peso/dimensión de los archivos que descarga el cliente en "Buena Calidad". Sin cambios de API ni esquema. | Aceptado: "buena" = copia con calidad baja y tope Full HD 1920 px; "alta" = original íntegro. | Aplicado |
| CC-25 | 2026-09-11 | Hallazgo técnico del equipo (en "Moderar aportes" las tarjetas se ven/can seleccionar pero sin miniatura ni vista previa) | El panel de moderación (`frontend-fotografo/js/moderation.js:renderizarPendientes`) mostraba la miniatura con `<img src="API/multimedia/{id}/vista-previa">` sin autenticación; como las colecciones colaborativas son privadas, el backend responde `401` (CC-21) y `onerror` ocultaba la imagen (las públicas no aplican aquí). Corrección: nueva `window.api.obtenerVistaPrevia(id)` en `frontend-fotografo/js/api.js` (fetch con header `Authorization: Bearer` → blob → objectURL, mismo patrón que el frontend cliente CC-21) y `renderizarPendientes` la usa, con revocación de objectURLs al re-renderizar y clic en la miniatura para abrir la vista previa completa en nueva pestaña. `node --check` OK. Sin cambios de backend ni esquema. | Alcance: frontend del fotógrafo (miniatura/vista previa de aportes pendientes); sin cambios de API, esquema ni backend. | Aceptada y verificada. | Aplicado |

> **Nota:** todo cambio con impacto en alcance, tiempo o costo debe quedar
> registrado aquí, aunque el equipo lo haya "resuelto" verbalmente en una reunión. Esto
> evita discusiones futuras sobre qué se acordó y por qué, y es evidencia concreta del
> trabajo de gestión del proyecto para la defensa final.
